const PARAMS_INDEXES = Object.freeze({
  FIRST: 1,
  SECOND: 2,
});

const COMMANDS = Object.freeze({
  ACTION: 'action',
  FILE: 'file',
  HELP: 'help',
});

const SHORT_COMMANDS = Object.freeze({
  ACTION: 'a',
  FILE: 'f',
  HELP: 'h',
});
